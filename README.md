# Diabetes-Prediction
End-to-end machine learning project for diabetes prediction using patient medical data. Includes data preprocessing, EDA, feature scaling, and model training using Logistic Regression and Random Forest. Models are evaluated using accuracy, precision, recall, F1-score, and confusion matrix, with scope for deployment and optimization.
